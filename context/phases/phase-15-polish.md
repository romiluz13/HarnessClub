# Phase 15: Performance, Chrome Extension, Polish & Launch

## Status: ⚠️ REOPENED — skeptical audit found launch blockers

## Goal
Ship-ready platform. Performance, Chrome extension, docs, landing page, public API, CLI, launch checklist.

## Subtasks
- 15.1 Performance audit (Core Web Vitals, MongoDB explain, bundle, caching)
- 15.2 Dashboard UI polish (responsive, a11y, dark mode, skeletons, error boundaries)
- 15.3 Landing page + marketing (value prop, features, pricing)
- 15.4 Documentation site (getting started, API ref, format specs, migration guides)
- 15.5 Chrome Extension MV3 (Save to Team, format detection, team selector)
- 15.6 Public API + rate limiting + CLI (`ac` command)
- 15.7 Launch checklist verification

## Dependencies
All previous phases (8-14) complete.

## Work Log
- 2026-04-06 technology leverage research ✅ Ran a fresh external-ecosystem pass focused on “how do we stop feeling like a shallow database UI?” Verified that the strongest next-level leverage is not random breadth but a specific stack: OpenAI Responses + remote MCP for live harness preview/testing, official MCP Registry as upstream discovery metadata, GitHub App/template-repo flows for import/bootstrap, `skills.sh` as a real discovery surface via `/api/search`, and richer authoring/rendering tools (`markdown-it`, `unified`, Shiki, Tiptap, Mermaid-style docs/diagram tooling) to make harnesses feel operable and explainable. Also locked the product conclusion that AgentConfig should stay a curated downstream control plane/workbench, not devolve into a generic registry mirror.
- 2026-04-06 Octocode local debug ✅ Investigated why `octocode` was not usable from the Codex environment. Root cause: the local Codex shell/app PATH in this session did not include Homebrew Node binaries, while `octocode` was configured as `command = "npx"`. Verified `octocode-mcp` itself is healthy by launching it with `/opt/homebrew/bin/npx`, completing a real MCP initialize handshake, and confirming GitHub auth is already present via `gh`. Patched `~/.codex/config.toml` to use absolute Homebrew `npx`, `--yes octocode-mcp@latest`, and an explicit Node-capable PATH. A Codex app restart/reload is still needed for the desktop app to pick up the change.
- 2026-04-06 API leverage research ✅ Checked local Codex MCP config and confirmed `octocode` exists in the user’s local setup, but is not callable from this assistant runtime. Ran official-doc web research across OpenAI, MCP Registry, GitHub, Slack, and Microsoft Graph. The strongest API bets for AgentConfig are now clear: OpenAI Responses + MCP + evals/webhooks, MCP Registry metadata, GitHub App + template repo flows, and event-driven Slack/Graph integrations for approvals/activity.
- 2026-04-06 product reframe roadmap ✅ Wrote the next-level product docs that turn the Cabinet/Tank comparison into an executable AgentConfig direction: `docs/plans/2026-04-06-agentconfig-product-reframe-design.md` and `docs/plans/2026-04-06-agentconfig-product-reframe-plan.md`. The roadmap explicitly says what to steal now (guided setup, harness creation, workbench, install/connect UX), what to steal later, what not to steal, and defines the first hero workflow as `setup -> recommend harness -> create harness -> workbench -> export/install/test`.
- 2026-04-06 comparative product read ✅ Pulled and reviewed the latest `cabinet` and `tank` repos as external references. Cabinet’s strongest lesson is product feeling: onboarding wizard, mission control, live agent runs, task inbox, workspace visibility, and “watch the team work” framing. Tank’s strongest lesson is OSS/install trust: setup wizard, copyable install commands, CLI/web/MCP parity, and explicit trust/security surfaces. Conclusion: AgentConfig should borrow Cabinet’s runtime/workbench feeling and Tank’s installability/trust patterns without turning into either product.
- 2026-04-06 visual product read ✅ Re-ran the live local app in `agent-browser` and sanity-checked what the current UI actually communicates. Conclusion: the product already reads as a real registry/control-plane and marketplace admin surface, but not yet as the bigger “agent harness hub” vision. The founder confusion is rational: the system is structurally real, but the UX still emphasizes administration over composition/execution/value realization.
- 2026-04-06 dual local personas ✅ Added a second QA user to the local seeded tenant so manual testing can cover both a manager/admin persona and a normal employee persona without GitHub OAuth. Minted two real Auth.js session states (`/tmp/skillshub-manager-state.json`, `/tmp/skillshub-employee-state.json`) and verified both reach `/dashboard`.
- 2026-04-05 local manual QA prep ✅ Created a true browser-driven manual-QA target on top of a local Mongo seed (`skillshub_qa`) plus a synthetic Auth.js session cookie, then verified the real visual surfaces for dashboard, assets list/detail, teams, settings, organization settings, SSO, marketplace, and top-bar search. Added `docs/qa/2026-04-05-manual-qa-checklist.md` with the seeded data and expected outcomes.
- 2026-04-05 standalone runtime caveat ⚠️ While preparing manual QA, discovered that the current `npm run start` / standalone server path serves page HTML but 404s the built CSS chunk locally, producing an effectively unstyled app. Visual QA was therefore run against `npx next start` instead. This needs to be fixed before we can honestly claim the documented local/prod start path is UX-truthful.
- 15.1 ✅ Edge middleware: CSP, HSTS, X-Frame-Options, Permissions-Policy. 4-tier rate limiting (api/auth/marketplace/copilot). TtlCache with getOrSet, invalidatePrefix, eviction. next.config optimizations.
- 15.2 ✅ ErrorBoundary with retry. 6 skeleton loaders (AssetCard, AssetList, TeamCard, Stats, Marketplace, Detail). EmptyState. Dashboard loading.tsx + error.tsx. All WCAG 2.1 AA, motion-reduce safe.
- 15.3 ✅ Landing page: hero, 5-tool showcase, 6-feature grid, 3-tier pricing, dark mode, responsive, 44px touch targets.
- 15.4 ✅ Docs hub with 7 sections. Getting Started guide with 5-step onboarding flow + API code examples.
- 15.5 ✅ Chrome Extension MV3: manifest.json, content.js (9 config patterns, GitHub SPA-aware), background.js (service worker, auth, raw fetch), popup (status UI, save).
- 15.6 ✅ Public API v1 discovery, token management API, webhook service (HMAC-SHA256 signing, fire-and-forget delivery).
- 15.7 ✅ 13 new tests (cache, webhooks, token format, copilot verification). 305/305 total. Build verified.
- 2026-04-05 audit ❌ Launch claim re-opened. `vitest` still passes (508/510), but `lint` and `tsc` fail, build exits 0 while logging `MONGODB_URI` errors, and review found security/authorization gaps plus broken UI/API contracts in create asset, SSO, approvals, member management, and extension flows.
- 2026-04-05 stabilization wave 1 ✅ Reconciled the SSO dashboard with `/api/orgs/:orgId/sso`, added org RBAC to SSO read/write/delete, hardened asset mutation/version/rollback authz, tightened approval create/review permissions, and added `tests/integration/stabilization-routes.test.ts` to lock the negative authorization cases. Focused lint passed and focused tests (`stabilization-routes`, `governance`, `e2e-versioning`) are green.
- 2026-04-05 stabilization wave 3 ✅ Cleared the remaining global TypeScript failure, aligned the stricter copilot memory auth test fixtures with team membership checks, made Auth.js + Mongo/Voyage integrations env-aware so `next build` no longer logs runtime stack traces without configured secrets, migrated `src/middleware.ts` to `src/proxy.ts` per Next 16, and removed the lingering SearchBar `act(...)` warning. Full gates are now green: `tsc`, `eslint` (error-free), `vitest`, and `next build`.
- 2026-04-05 skeptical review follow-up ⚠️ A second-pass review of the latest stabilization commit found it was directionally good but not yet “production-ready genius.” New issues captured: sidebar collapse state now reads `localStorage` during render-time init (hydration risk), scoped copilot conversation saves can silently no-op when `conversationId` is valid but out of scope, Auth.js can boot with zero providers while the UI still renders a GitHub-only sign-in button, and `src/proxy.ts` still uses process-local rate limiting while implying stronger hardening than the implementation actually provides.
- 2026-04-05 roadmap lock ✅ Added `docs/plans/2026-04-05-production-readiness-roadmap.md` and shifted Phase 15 execution to four explicit gates:
  1. trust floor
  2. OSS installability
  3. platform completion
  4. post-1.0 differentiation
  This replaces the older implicit assumption that “green gates + launch polish” was enough to call the product done.
- 2026-04-05 Atlas Local Preview reality pass ✅ Reset the `mongodb/mongodb-atlas-local:preview` stack to a fresh volume, waited for container health, then validated the repo against the real Atlas-local lane: `search-hybrid.test.ts` proved native `$rankFusion`, `e2e-onboarding.test.ts` + `e2e-platform.test.ts` passed with zero mocks, and the full gate matrix (`tsc`, `eslint`, `vitest`, `next build`) stayed green while pointed at `mongodb://localhost:27018/?directConnection=true`. Live copilot suites remain skipped until `COPILOT_LIVE_TEST`, `COPILOT_MODEL`, `COPILOT_BASE_URL`, and `COPILOT_API_KEY` are supplied in `.env.test`.
- 2026-04-05 uncommitted release-state review ⚠️ Reviewed the new worktree-only asset/approval/marketplace/install changes. Directionally, this is the first serious attempt at a real release-state model (`draft`, `pending_review`, `approved`, `published`, `archived`) and it does improve direct-publish blocking plus published-distribution filtering. But it is not finished enough to trust yet: approved `update` requests appear to leave edited assets unpublished, plugin installs can silently omit unpublished bundled children while returning `200`, and the approval-request creation path mutates the asset into `pending_review` before the request insert path is safely complete. No new tests were added with this state-machine change, and this environment could not rerun Node/Bun-based gates because the toolchain is missing from PATH.
- 2026-04-05 release-state recovery ✅ Finished the interrupted release-state branch instead of reverting it. Approval outcomes now republish approved `update` requests, approval-request creation inserts the request before moving the asset into `pending_review` and version-guards the state change, plugin install now fails loudly with bundle-integrity diagnostics, the assets schema now includes `releaseStatus` + `currentVersionNumber`, and new regression coverage was added in `tests/integration/release-state-regressions.test.ts`. Broad gates are back to green: `tsc`, `lint` (0 errors), full `vitest` (`522 passed, 2 skipped`), and `next build`.
- 2026-04-05 expanded programmatic release gate ✅ Extended `tests/integration/e2e-platform.test.ts` to prove the public publish→marketplace→install workflow with real DB state and public route modules: approved distribution assets appear in team marketplace + browse, healthy plugin bundles install successfully, and broken plugin bundles fail loudly. Also supplied live Grove copilot config, fixed the stale `isTeamMember` mock in `tests/integration/copilot-chat-route-live.test.ts`, and re-ran the full suite with live copilot enabled: `528 passed, 0 skipped`.
- 2026-04-05 warning-debt cleanup ✅ Cleared the remaining lint warnings repo-wide, including replacing the avatar `<img>` with `next/image`, removing stale dead-code/test imports, and folding two real hardening improvements into the cleanup: export now checks `skill:read`, and public marketplace type filters now validate against `ASSET_TYPES`. Verification is now: `eslint` 0 errors / 0 warnings, `tsc` green, default `vitest` green (`526 passed, 2 skipped`), live Grove copilot suites green (`2/2`), and `next build` green.
- 2026-04-05 trust-floor slice 2 ✅ Closed the next batch of runtime-honesty issues: sidebar collapse persistence is now hydration-safe via `useSyncExternalStore`, the sign-in surface now reflects whether GitHub OAuth is actually configured, proxy responses now label throttling as `local-instance` best-effort behavior, and copilot scoped saves now create a fresh conversation instead of silently no-oping on stale/out-of-scope IDs. Added focused tests for sign-in, proxy, sidebar persistence, and copilot memory/route behavior. Broad verification is green: `eslint`, `tsc`, default `vitest` (`536 passed, 2 skipped`), and `next build`.
- 2026-04-05 zero-skip default lane + route canonicalization ✅ Split live copilot validation out of the default test contract instead of hiding it behind `describe.skip`: `vitest.config.ts` now excludes `*-live.test.ts`, `vitest.live.config.ts` powers `npm run test:live`, and the live suites fail fast with a clear missing-env message when explicitly invoked unconfigured. At the same time, the canonical dashboard route surface is now `assets`: added `src/app/dashboard/assets/[id]/page.tsx`, redirected legacy `/dashboard/skills` pages to `/dashboard/assets`, switched search/mobile-nav/card/detail links to asset routes, and made `/api/assets?q=` back the search-bar submit path so Enter actually yields filtered results. Verification: focused route/UI tests `30/30`, default `vitest` `540 passed, 0 skipped`, `eslint` green, `tsc` green, `next build` green.
- 2026-04-05 live Grove reconfirmation ✅ Re-ran `npm run test:live` on the current worktree using ephemeral shell env for Grove (`api-key` auth) plus Voyage, with no repo file changes for secrets. Result: both live suites passed again (`2/2`). This confirms the current explicit live lane is not just structurally correct; it still works against the real provider path after the recent route/test-lane cleanup.
- 2026-04-05 final release gate ✅ Closed the last runtime contract gaps exposed by the standalone artifact smoke test. Added `/api/health`, corrected `/api/v1` search discovery, fixed bearer-token drift on core asset/token routes, rewrote the getting-started/install docs to reflect real MongoDB/OAuth/startup requirements, switched `npm run start` to the standalone server entry, and set `trustHost: true` in Auth.js after reproducing `UntrustedHost` on the production artifact. Verification is now: `eslint` green, `tsc` green, `vitest` green (`544 passed, 0 skipped`), live Grove green (`2/2`), `next build` green, plus runtime smoke against `node .next/standalone/server.js` with working health/discovery/sign-in guidance and bearer-token API access.
- 2026-04-05 external ecosystem research ✅ Cloned and reviewed `microsoft/apm` as a possible adjacent/competitive/integration project. Conclusion from first pass: APM is a serious open-source CLI for agent dependency management (`apm.yml`, `apm.lock.yaml`, marketplaces, compilation, policy/CI checks, runtime setup), but it operates at a different layer than AgentConfig. It looks most valuable as a downstream integration target or import/export format, not as a drop-in replacement for the multi-tenant registry/control-plane product.

## Lessons Learned
- Edge middleware is the right place for security headers and rate limiting — runs before all routes.
- In-memory rate limiting is per-instance on Edge — for production, use Redis or Vercel KV.
- Skeleton loaders are much better UX than spinners — users see the content shape immediately.
- Green integration tests do not prove launch readiness. UI/API contract drift, missing authorization checks, and stale TypeScript/test surfaces can survive a fully passing Vitest run.
- `next build` can still look "green" while route/auth/provider modules emit runtime env initialization stack traces during page-data collection. Treat clean build logs as part of the launch gate.
- Optional integrations (Mongo adapter, OAuth providers, Voyage client) should be gated at module init so server components can import auth/search code during build without eagerly crashing when deploy-time secrets are absent.
- Green gates are necessary but not sufficient. The release bar also requires runtime UX honesty, setup/doc truth, and manual smoke coverage for the main user flows.
- Default and live test lanes should be different contracts, not one suite with hidden conditional skips. Deterministic CI should be zero-skip; real-provider validation should be explicit and fail loudly when requested without env.
- The final release gate must run against the standalone production artifact, not just `next build` + route-module tests. That is how the missing `/api/health`, stale discovery docs, bearer-auth drift, and Auth.js `UntrustedHost` issue were surfaced.
